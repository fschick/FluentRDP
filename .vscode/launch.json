{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Publish MSI",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}\\Build\\scripts\\fluentrdp.msi.ps1",
            "args": ["-version", "0.0.1", "-publishFolder", "${workspaceFolder}\\FluentRDP.0.0.1"],
        },
        {
            "name": "Publish MSIX",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}\\Build\\scripts\\fluentrdp.msix.ps1",
            "args": ["-version", "0.0.1", "-publishFolder", "${workspaceFolder}\\FluentRDP.0.0.1"],
        }
    ]
}